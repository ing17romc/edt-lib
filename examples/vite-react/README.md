# `examples/vite-react`

Minimal Vite consumer for `edt-lib`.

## Install

```bash
cd examples/vite-react
npm install
```

## Run

```bash
npm run dev
```

This example uses:

- `import 'edt-lib/styles.css'`
- package-root imports from `edt-lib`
- `ThemeProvider` at the app root
