---
mode: 'agent'
description: 'Fix unit test errors. Runs npm run test, fixes failures and removes console.error/console.warn.'
---

Run `npm run test`, fix all test failures and remove any `console.error` or `console.warn` messages that appear during execution.
