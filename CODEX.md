# CODEX.md

Use [docs/ai/LLMS.md](./docs/ai/LLMS.md) as the primary repo contract.

Codex-specific reminder:

- do not ship consumer examples that import from `src/` or `dist/`
- treat `src/index.ts` and `src/components/index.ts` as the public export source of truth
