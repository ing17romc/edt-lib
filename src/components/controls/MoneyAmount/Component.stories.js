import React from 'react'

import Component from '.'

export default {
	title: 'Building/MoneyAmount',
	component: Component
}

const Template = (args) => <Component {...args} />

export const Default = Template.bind({})
