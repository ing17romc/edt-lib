/**
 * Tipos y contratos para el componente Message.
 */

/**
 * Props para el componente Message.
 * Actualmente no recibe props, pero se deja la interfaz para futuras extensiones.
 */
export type MessageProps = Record<string, never>;
