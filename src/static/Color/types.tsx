/**
 * Tipos y contratos para el componente Color.
 */

/**
 * Props para el componente Color.
 * Actualmente no recibe props, pero se deja la interfaz para futuras extensiones.
 */
export type ColorProps = Record<string, never>;
