/**
 * Tipos y contratos para el componente Typography.
 */

/**
 * Props para el componente Typography.
 * Actualmente no recibe props, pero se deja la interfaz para futuras extensiones.
 */
export type TypographyProps = Record<string, never>;
