// Tipos para el componente Icons

/**
 * Representa una categoría de íconos y su lista de nombres.
 */
export interface IconCategory {
  category: string;
  icons: string[];
}

/**
 * Props para el componente Icons.
 * Actualmente el componente no recibe props, pero se deja la interfaz para futura extensión.
 */
// No se requiere interfaz vacía para IconsProps
